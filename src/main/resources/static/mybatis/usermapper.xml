<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pintalk.user.mapper.UserMapper">


    <select id="getUserListSeq" resultType="int">
        SELECT USERMEMBER_SEQ.NEXTVAL
        FROM DUAL
    </select>

    <select id="getUserList" resultType="UserMember">
        SELECT *
        FROM USER_MEMBER
        WHERE 1=1
          AND STATUS = 'Y'
    </select>


    <insert id="userMemberInsert"  parameterType="HashMap">
        INSERT INTO USER_MEMBER(NO,ID,NAME,TYPE,SSN,SSN1,SSN2,PHONE1,PHONE2,PHONE3,GENDER,REG_DATE,UPDATE_DATE,STATUS,PASSWORD
        <if test="email != null">
            , EMAIL
        </if>
        <if test="address1 != null">
            , ADDRESS1
        </if>
        <if test="address2 != null">
            , ADDRESS2
        </if>
        <if test="job_key != null">
            , JOB_KEY
        </if>
        )
        VALUES (
        #{user_no}
        ,#{id}
        ,#{name}
        ,#{type}
        ,#{ssn}
        ,#{ssn1}
        ,#{ssn2}
        ,#{phone1}
        ,#{phone2}
        ,#{phone3}
        ,#{gender}
        ,SYSDATE
        ,SYSDATE
        ,'Y'
        ,#{password}
        <if test="email != null">
            , #{email}
        </if>
        <if test="address1 != null">
            , #{address1}
        </if>
        <if test="address2 != null">
            , #{address2}
        </if>
        <if test="job_key != null">
            , #{job_key}
        </if>
        )

    </insert>

</mapper>